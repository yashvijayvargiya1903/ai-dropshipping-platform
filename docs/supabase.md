# Supabase integration

Project: drop-platform
Region: ap-south-1 (Mumbai)

The production database, RLS policies, Auth profile trigger, catalog seed and publishable client configuration are now provisioned.

The publishable key is safe for client use. Never expose service-role keys or database passwords in the storefront.

Next deployment configuration should provide the Supabase URL and publishable key through the deployment environment where supported.
