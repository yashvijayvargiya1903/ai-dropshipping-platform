const transitions={PENDING_PAYMENT:["PAID","CANCELLED"],PAID:["FULFILLMENT_PENDING","REFUND_PENDING"],COD_CONFIRMED:["FULFILLMENT_PENDING","CANCELLED"],FULFILLMENT_PENDING:["SUPPLIER_SUBMITTED","SUPPLIER_ACTION_REQUIRED"],SUPPLIER_SUBMITTED:["SUPPLIER_CONFIRMED","SUPPLIER_ACTION_REQUIRED"],SUPPLIER_CONFIRMED:["SHIPPED","CANCEL_REQUESTED"],SHIPPED:["IN_TRANSIT"],IN_TRANSIT:["OUT_FOR_DELIVERY","RETURN_REQUESTED"],OUT_FOR_DELIVERY:["DELIVERED"],DELIVERED:["RETURN_REQUESTED"],RETURN_REQUESTED:["RETURN_IN_TRANSIT","MANUAL_REVIEW"],RETURN_IN_TRANSIT:["RETURNED"],RETURNED:["REFUND_PENDING","EXCHANGE_REQUESTED"],REFUND_PENDING:["REFUNDED"]};export function canTransition(from,to){return (transitions[from]||[]).includes(to)}export function transition(order,to,actor="system"){if(!canTransition(order.status,to))throw new Error(`Invalid transition: ${order.status} -> ${to}`);return {...order,status:to,audit:[...(order.audit||[]),{from:order.status,to,actor,at:new Date().toISOString()}]}}